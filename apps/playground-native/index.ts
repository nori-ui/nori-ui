// expo-router 6 supplies its own root component that wires
// `app/_layout.tsx` and the file-based router into React Native's
// `AppRegistry`. We just import its entry — no `registerRootComponent`
// call needed.
import 'expo-router/entry';
