import { Box, Text } from '@nori-ui/core';

export default function BoxBasic() {
    return (
        <Box className="p-4 bg-primary-50 rounded-md">
            <Text>Box with padding and background</Text>
        </Box>
    );
}
