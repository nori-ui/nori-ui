---
title: Toggle
description: Two-state button — both standalone and as a clustered ToggleGroup with single- or multi-select semantics.
since: 0.3.0
tags: [control, form, toolbar]
platform: both
---

import { BundleSize } from '@/components/bundle-size';
import { Callout } from 'fumadocs-ui/components/callout';
import { Preview } from '@/components/preview';
import { PropsTable } from '@/components/props-table';

<BundleSize component="Toggle" />

## At a glance

- `Toggle` is a single bistable button — think of the **B** / **I** / **U** buttons in a rich-text toolbar, where each one carries its own on/off visual.
- `ToggleGroup` clusters several toggles with shared selection semantics: `type="multiple"` for any-of-N (formatting marks), `type="single"` for one-of-N (text alignment).
- Roving-tabindex keyboard nav follows the WAI-ARIA radiogroup pattern: `Arrow` keys move focus between items, `Home` / `End` jump to the ends, `Space` / `Enter` toggles the focused item.
- ARIA: every item exposes `role="button"` + `aria-pressed`. The group exposes `role="group"` (multiple) or `role="radiogroup"` (single).

<Callout title="Toggle vs Switch vs Checkbox">
    These three controls look similar but ship different semantics — pick the one that matches the *meaning* of the choice, not the shape of the visual.

    - **Toggle** — a button with on/off state. Use it for toolbar-style actions where the control *is* a button (Bold, Pin, Mute). Lives outside `<form>`.
    - **Switch** — a setting that takes effect immediately. Use it for preference rows ("Dark mode", "Email notifications"). Reads as `role="switch"`.
    - **Checkbox** — a value selected as part of a form. Use it when the user reviews several choices and submits them together. Reads as `role="checkbox"`.

    If the control sits in a toolbar, reach for `Toggle`. If it lives in Settings, reach for `Switch`. If it's part of a form, reach for `Checkbox`.
</Callout>

## Standalone Toggle

A single bistable button with controlled (`pressed` / `onPressedChange`) and uncontrolled (`defaultPressed`) modes.

<Preview name="toggle-basic" />

## Multiple selection (formatting toolbar)

`type="multiple"` — `value` is `string[]`. Clicking adds or removes an item from the array.

<Preview name="toggle-group-multiple" />

## Single selection (alignment)

`type="single"` — `value` is `string | undefined`. Clicking sets it; clicking the active item again clears it.

<Preview name="toggle-group-single" />

## Props

### `<Toggle>`

<PropsTable component="Toggle" />

### `<ToggleGroup>`

<PropsTable component="ToggleGroup" />

### `<ToggleGroupItem>`

<PropsTable component="ToggleGroupItem" />
